# 项目维护规则

本文件适用于仓库根目录及其全部子目录。后续任何代码修改、重构、上游同步、冲突解决和发布操作都必须遵守以下规则。

## 许可证逻辑保护

- 必须保持本地现有的永久许可证授权行为，不得删除、降级、限制或改写该行为。
- 不得修改任何会直接或间接影响许可证有效性、永久授权、到期判断、套餐能力、服务器配额、功能授权、授权状态持久化或 `xray_authorized` 处理的代码。
- 不得用原仓库的许可证实现覆盖本地许可证代码，也不得在解决合并冲突时接受会改变本地许可证行为的上游版本。
- 修改看似无关的认证、配置、启动、升级、通信或数据模型代码前，必须先检查其是否会影响许可证流程；如有影响，保留本地行为并停止对许可证相关部分的修改。
- 不得通过格式化、重命名、移动文件或重构等方式间接改动许可证相关逻辑。
- 验证修改时，必须确认永久许可证授权仍保持有效，且既有授权能力没有回退。

## 上游代码同步

- 原仓库仅作为获取新功能和修复的上游来源：`https://github.com/iluobei/mmw-agent`。
- 可以拉取并合并原仓库更新，但合并前必须审查差异中所有许可证、授权、认证、配置和升级相关改动。
- 发生冲突时，许可证及授权相关代码始终以本地版本为准；只合并与许可证行为无关的新功能、修复和兼容性变更。
- 合并完成后，必须搜索并检查许可证相关符号、更新地址、发布地址和 CDN 地址，避免上游配置被带回。
- 不得把本地受保护的许可证实现反向替换成上游实现。

## 仓库、发布与更新地址

- 本项目的唯一发布和更新仓库为：`https://github.com/nuxproio/mmw-agent`。
- 新增或修改版本检测、自更新、安装脚本、发布脚本、二进制下载、签名下载、Release API、源码链接、问题反馈链接或镜像回退时，必须使用 `nuxproio/mmw-agent`。
- 不得使用 `iluobei/mmw-agent` 的 Release、Raw、API、自更新地址、下载地址、CDN 地址或任何指向其发布制品的代理地址。
- CDN 或 GitHub 代理仅可代理 `https://github.com/nuxproio/mmw-agent` 下的本项目制品，不得将原仓库作为回退源。
- 文档中可以保留原仓库地址用于说明上游来源，但不得将其描述或配置为当前项目的安装、发布、下载或更新来源。

## 修改检查清单

提交修改前至少执行以下检查：

1. 确认变更未触及或改变许可证及永久授权逻辑。
2. 确认上游合并没有覆盖本地许可证实现。
3. 搜索 `iluobei/mmw-agent`，确认它只作为上游来源说明存在。
4. 检查安装、发布、升级、下载和 CDN/代理地址，确认最终目标均为 `nuxproio/mmw-agent`。
5. 运行与变更范围对应的测试，并验证永久许可证授权行为未发生回退。
